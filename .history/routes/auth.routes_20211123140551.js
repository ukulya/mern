const {Router} = require('express')
const router = Router();
// middleware

// /api/auth/register
router.post('/register',async(req,res) => {

})

// /api/auth/register
router.post('/register',async(req,res) => {

})

module.exports = router;